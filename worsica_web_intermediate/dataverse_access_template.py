DATAVERSE_DOMAIN = 'localhost'
DATAVERSE_BASE_URL = 'http://' + DATAVERSE_DOMAIN + ':8080'  # url of the dataverse instance
DATAVERSE_ALIAS = '...'  # alias name after creating the dataverse
DATAVERSE_ROOT_ALIAS = 'worsica'  # provide a root alias name as starting point for building dataverse collection tree
DATAVERSE_API_TOKEN = '...'  # generate api token on the dataverse interface
DATAVERSE_PATH_TO_WORSICA_FRONTEND = 'http://frontend'  # provide the url for the frontend (to generate urls for products on metadata)
